const router=require('express').Router();const auth=require('../middleware/auth');const role=require('../middleware/requireRole');const{db}=require('../config/firebase');const{v4:uuid}=require('uuid');
router.get('/',auth,async(req,res)=>{try{const snap=await db().collection('students').where('schoolId','==',req.schoolId).get();res.json({success:true,data:snap.docs.map(d=>({id:d.id,...d.data()}))});}catch(e){res.status(500).json({success:false,code:'server_error'});}});
router.post('/',auth,role('director','teacher'),async(req,res)=>{try{const{name,email,password,classId,className,phone,idNumber}=req.body;if(!name||!email||!password)return res.status(400).json({success:false,code:'missing_fields'});const lEmail=email.trim().toLowerCase();const exists=await db().collection('students').where('email','==',lEmail).limit(1).get();if(!exists.empty)return res.status(400).json({success:false,code:'email_taken'});const schoolDoc=await db().collection('schools').doc(req.schoolId).get();const id=uuid();const student={id,name:name.trim(),email:lEmail,password:password.trim(),role:'student',status:'approved',schoolId:req.schoolId,schoolName:schoolDoc.data()?.name||'',classId:classId||'',className:className||'',phone:phone||'',idNumber:idNumber||'',joinedAt:new Date().toISOString(),createdBy:req.userId};await db().collection('students').doc(id).set(student);res.json({success:true,data:student});}catch(e){res.status(500).json({success:false,code:'server_error'});}});
router.patch('/:id',auth,async(req,res)=>{try{const u={...req.body};delete u.id;delete u.role;delete u.schoolId;await db().collection('students').doc(req.params.id).update(u);res.json({success:true});}catch(e){res.status(500).json({success:false,code:'server_error'});}});
router.delete('/:id',auth,async(req,res)=>{try{await db().collection('students').doc(req.params.id).delete();res.json({success:true});}catch(e){res.status(500).json({success:false,code:'server_error'});}});
router.post('/:id/approve',auth,async(req,res)=>{try{await db().collection('students').doc(req.params.id).update({status:'approved'});res.json({success:true});}catch(e){res.status(500).json({success:false,code:'server_error'});}});
router.post('/:id/reject',auth,async(req,res)=>{try{await db().collection('students').doc(req.params.id).update({status:'rejected'});res.json({success:true});}catch(e){res.status(500).json({success:false,code:'server_error'});}});
router.post('/:id/reset-password',auth,async(req,res)=>{try{if(!req.body.password)return res.status(400).json({success:false,code:'missing_password'});await db().collection('students').doc(req.params.id).update({password:req.body.password.trim()});res.json({success:true});}catch(e){res.status(500).json({success:false,code:'server_error'});}});
router.post('/update-profile',auth,async(req,res)=>{try{const{name,phone,profileImageBase64}=req.body;const u={};if(name)u.name=name;if(phone!==undefined)u.phone=phone;if(profileImageBase64)u.profileImageBase64=profileImageBase64;let col,docId;if(req.role==='student'){col='students';docId=req.userId;}else if(req.role==='director'){col='schools';docId=req.schoolId;if(name)u.teacherName=name;if(profileImageBase64)u.teacherPhotoBase64=profileImageBase64;}else{col='teacher_accounts';docId=req.userId.replace('teacher-','');}await db().collection(col).doc(docId).update(u);res.json({success:true});}catch(e){res.status(500).json({success:false,code:'server_error'});}});
module.exports=router;